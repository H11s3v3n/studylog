# 虚拟机配置代理

### 宿主机

1. Clash verge打开局域网连接
2. 查看nat网段

### 虚拟机

1. 设置-网络-代理-socks主机设置宿主机nat地址和端口

#####  docker代理配置

1. 创建 systemd 配置目录（如果没有）：

```sudo mkdir -p /etc/systemd/system/docker.service.d```

2. 创建配置文件：

```sudo nano /etc/systemd/system/docker.service.d/http-proxy.conf```

3. 内容：

```[Service]
Environment="HTTP_PROXY=http://192.168.32.1:7890"
Environment="HTTPS_PROXY=http://192.168.32.1:7890"
Environment="NO_PROXY=localhost,127.0.0.1"
```

4. 重载 systemd 并重启 Docker：

```sudo systemctl daemon-reexec
sudo systemctl daemon-reload
sudo systemctl restart docker
```
